# BÁO CÁO — Đánh giá hạ tầng fine-tune Qwen3-0.6B theo phê bình

**Ngày:** 12/08/2026
**Đối tượng:** Hệ thống fine-tune LoRA `scripts/finetune_qwen3.py` + `run_finetune.bat`
**Mục đích:** Tổng hợp và đối chiếu lời phê bình, kèm hướng xử lý đề xuất.

---

## 1. Hiện trạng — xác minh lại con số

| Chỉ số | Giá trị hiện tại | Ghi chú |
|---|---|---|
| Số mẫu dataset | **15,765** | `merged_vn_uncensored.jsonl` (đã xác minh) |
| Batch hiệu dụng | **8** (2×4×1) | per_device=2 × grad_accum=4 × 1 GPU |
| 1 epoch đầy đủ | ≈ **1,971 steps** | 15765 ÷ 8 |
| max_steps mặc định | **500** | phê bình nhắc 300 (số nhập gần nhất) |
| Coverage với 500 steps | **25%** | = 500 ÷ 1971 |
| Coverage với 300 steps | **15%** | = 300 ÷ 1971 — **đúng như phê bình** |
| logging_steps | **10** | → 50–197 lần log mỗi run |
| warmup | warmup_steps=5 (≈1%) | decay linear về 0 |

---

## 2. Đánh giá từng điểm phê bình

### 2.1. Học vẹt / học thiếu — **ĐÚNG**
Dùng `max_steps` cố định nhỏ hơn số steps của 1 epoch khiến model **không thể cover trọn 100% dữ liệu dù chỉ 1 lần**. Với 300 steps → chỉ "nhìn" 15% tập dữ liệu bị ép dừng giữa chừng, không đủ tạo dấu ấn tri thức (fingerprinting) đặc trưng trên toàn bộ tập.

- **Sai sót nhỏ của phê bình:** con số (300 vs 500 mặc định) — nhưng bản chất đúng.
- **Đúng về toán:** coverage = max_steps ÷ 1971.

### 2.2. Đói tài nguyên — **ĐÚNG MỘT PHẦN**
Model 0.6B load 4-bit rất nhẹ, VRAM 24GB thừa thãi. Batch hiệu dụng = 8 là an toàn quá mức, giảm scalability. Có dư địa tăng lên 8–16 và giảm grad_accum còn 1–2.

- Lưu ý: với model siêu nhỏ, lợi ích của batch rất lớn có giới hạn; nhưng nhận định "VRAM nhàn rỗi, không ép tải" là đúng về nguyên tắc.

### 2.3. Cảnh báo return_dict chưa dọn — **ĐÚNG**
Tiếng ồn từ `config.use_return_dict` not-None (phổ biến khi kết hợp FlashAttention 2 / một số sub-layer). Không ảnh hưởng chất lượng nhưng cần dọn để log sạch.

### 2.4. Lịch trình LR rơi tự do — **ĐÚNG MỘT PHẦN**
`warmup_steps=5` (~1%) quá ngắn; với tuyến `linear` sẽ decay về 0 sớm và nhanh. `warmup_ratio=0.1` chuẩn công nghiệp hơn.

- Đây là **thiết kế chưa chuẩn**, không phải "lỗi hệ thống" như lời văn phê bình — mức độ nói hơi quá nhưng đề xuất sửa là hợp lý.

### 2.5. Log lắt nhắt 10 steps — **ĐÚNG**
Với 1 epoch (1,971 steps) sẽ log ~197 lần, tạo I/O dư thừa. Tăng `logging_steps=50` hợp lý.

---

## 3. Tổng kết mức độ chính xác của phê bình

**Đúng ~80% về bản chất kỹ thuật.** Sai một con số (300 vs 500), và cảnh báo "lỗi hệ thống" cho warmup hơi nặng lời (thực chất là thiết kế chưa chuẩn).

Bài học cốt lõi: **không nên set cứng max_steps khi dataset nhỏ hơn khả năng một epoch** — chuẩn là chạy theo `num_train_epochs`.

---

## 4. Hướng xử lý đề xuất

### 4.1. Đổi chiến lược steps (ưu tiên cao)
- Bỏ `max_steps` khi train chính thức; đặt **`num_train_epochs=1`** để cày 100% dữ liệu ít nhất 1 lần.
- **Giữ `max_steps` chỉ cho chế độ thử nghiệm nhanh** (thêm flag `--test` riêng, tách khỏi luồng chính).

### 4.2. Bơm tải GPU (ưu tiên cao)
- `per_device_train_batch_size=8` (hoặc 16).
- `gradient_accumulation_steps=1` (hoặc 2).
- → Batch hiệu dụng: 8, batch thực tế/step lớn hơn, tận dụng VRAM 24GB, cập nhật trọng số nhanh hơn.

### 4.3. Bảo vệ Learning Rate (ưu tiên trung bình)
- Thay `warmup_steps=5` bằng **`warmup_ratio=0.1`**.
- Giữ `lr_scheduler_type="linear"`.

### 4.4. Dọn rác I/O (ưu tiên thấp)
- `logging_steps=50`.
- Xử lý `return_dict`: set rõ `config.use_return_dict` hoặc truyền `return_dict=True/False` ở kernel/forward, dẹp cảnh báo.

### 4.5. Điểm còn tranh luận (kiến nghị thêm)
- **Qwen3-0.6B là model thinking**: nếu dataset chỉ có câu trả lời trực tiếp (không có ` thinking`), việc tăng epoch giúp thuộc data nhưng **không dàn đều fingerprint theo phân phối thực** của model. Nên xem xét xử lý ` thinking` song song — liên quan đến vấn đề template đã ghi nhận trước đó, không nằm trong lời phê bình.

---

## 5. Trạng thái

- **Chưa code** theo yêu cầu — báo cáo chỉ tổng hợp và đề xuất.
- Nhóm 4.1–4.4 có thể áp dụng trực tiếp vào `finetune_qwen3.py` khi được duyệt.

*Kết thúc báo cáo.*